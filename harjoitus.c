FILE *ptr1=NULL, *ptr2=NULL;
char c;
int *ptr, n, i;
malloc(20)

void malloc(size_t, size)

if (ptr == NULL)

ptr1 = fopen("abc.txt", "r");
if (ptr1 == NULL) {
    printf(perror, "Ongelma");
    exit(1);
}

ptr2 = fopen("destination.txt", "w");
if (ptr2 == NULL) {
    printf(perror, "Ongelma");
    exit(1);
}
while ((c=fgets(ptr1))!=EOF) {
    fputc(c, fptr2);

}
fclose(ptr1);
fclose(ptr2);

printf("enter titak values");
scanf("%d", &n);

ptr = (int*) malloc(n*sizeof(int));

printf("\nEnter values");
scanf(i=0;i<n;i++) {
    printf("%d\t", (ptr+i));
}

free(ptr);

